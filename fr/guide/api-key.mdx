---
title: "Créer et protéger une clé API"
description: "Créez une clé NexusAPI limitée, stockez-la correctement et renouvelez-la en cas de risque."
---

Une clé API identifie l'appelant et détermine les groupes de modèles accessibles. Créez une clé par projet ou environnement afin de limiter les dépenses, rapprocher l'usage et révoquer un accès sans interrompre tous les services.

## Créer une clé

1. Connectez-vous à la [console NexusAPI](https://nexusapi.link).
2. Ouvrez **Gestion des jetons**, puis **Ajouter un jeton**.
3. Donnez-lui un nom clair, par exemple `production-api` ou `codex-local`.
4. Sélectionnez le groupe qui contient le modèle souhaité.
5. Définissez une limite et, si nécessaire, une date d'expiration, puis enregistrez.

Copiez la clé immédiatement : sa valeur complète peut n'être affichée qu'une seule fois.

## À décider avant la création

| Élément | Rôle |
| --- | --- |
| Groupe de jetons | Détermine les modèles, canaux et règles accessibles. |
| Limite de crédit | Réduit l'exposition d'un projet ou d'un test. |
| Expiration | Utile pour un accès temporaire. |
| Restriction de modèle / liste d'IP | À activer seulement si votre cas l'exige. |

## Protéger la clé

- Utilisez une variable d'environnement, un gestionnaire de secrets ou le coffre du système.
- Ne la mettez jamais dans le front-end, un dépôt public, une capture, un chat ou une URL.
- Ne partagez pas une clé entre équipes ou services sans raison ; des clés séparées facilitent l'audit.
- En cas de fuite présumée, désactivez ou supprimez l'ancienne clé, créez-en une autre et mettez à jour les déploiements.

## Tester avant d'intégrer

```bash
curl 'https://nexusapi.link/v1/models' \
  -H 'Authorization: Bearer YOUR_API_KEY'
```

Ce test confirme que la clé est reconnue et montre les modèles visibles. Pour choisir l'API correcte, consultez la [matrice des capacités](/fr/developer/capability-matrix).

Pour une erreur 401, 403 ou de modèle indisponible, vérifiez l'état de la clé, le groupe, le crédit, les restrictions IP et le préfixe `Bearer`, puis conservez le Request ID.

## Clé API et autres identifiants

| Identifiant | Usage |
| --- | --- |
| Clé API | Appelle les API de modèles : Chat Completions, Responses, Messages, Images et `/v1/models`. |
| Session / cookie de console | Sert uniquement à ouvrir la console dans le navigateur ; ce n’est pas un identifiant d’API de modèle. |

Ne placez jamais un identifiant dans les paramètres de l’URL et ne transmettez jamais sa valeur complète au support.
