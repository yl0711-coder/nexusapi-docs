---
title: "Hướng dẫn cài đặt và sử dụng CC Switch"
---

CC Switch là công cụ quản lý cấu hình AI CLI có thể quản lý tập trung các cấu hình API cho các công cụ AI CLI như Claude Code và Codex. Sau khi cấu hình, bạn có thể chuyển đổi giữa các nhà cung cấp khác nhau trong CC Switch, sau đó khởi chạy công cụ CLI tương ứng bằng NexusAPI.

Bài viết này giải thích cách cấu hình thủ công NexusAPI trong công cụ CC Switch.

## 1. Chuẩn bị trước khi sử dụng

Vui lòng chuẩn bị các thông tin sau trước khi cấu hình:

| Dự án | Điền nội dung |
| --- | --- |
| Địa chỉ NexusAPI | `https://nexusapi.link` |
| Khóa API | Các token được tạo bởi bảng điều khiển NexusAPI thường có định dạng sau: `sk-xxx` |
| Mã mẫu | Các ID mô hình có sẵn trong nhóm API Key hiện tại |

Lộ trình thu thập API Key:

```text
NexusAPI 控制台
-> 令牌管理
-> 创建或复制 API Key
```

Lưu ý:

1. API Key phải được tạo bằng bảng điều khiển NexusAPI.
2. Nếu khóa đã sao chép bị thiếu `sk-` Tiền tố, cần được thêm trong quá trình cấu hình `sk-xxx`。
3. ID mô hình phải chọn các mô hình có sẵn trong nhóm token hiện tại; đừng chỉ nhìn vào danh sách mô hình toàn trang trên trang.

## 2. Cài đặt Công tắc CC

### 2.1 macOS

Khuyến nghị cài đặt bằng Homebrew:

```bash
brew install --cask cc-switch
```

Sau khi cài đặt, mở CC Switch trong ứng dụng.

### 2.2 Windows

Truy cập trang Phát hành GitHub của CC Switch:

```text
https://github.com/farion1231/cc-switch/releases
```

Tải về `.msi` Cài đặt gói và làm theo hướng dẫn trong trình cài đặt để hoàn tất quá trình cài đặt.

Nếu bạn sử dụng phiên bản di động, bạn cũng có thể tải về `.zip` Giải nén tệp và chạy trực tiếp.

### 2.3 Linux

Truy cập trang Phát hành GitHub của CC Switch:

```text
https://github.com/farion1231/cc-switch/releases
```

Tải xuống theo hệ thống `.deb` Cài đặt gói hoặc `.AppImage` Tài liệu.

<Warning>
**Chỉ tải về từ các kênh chính thức**

Mã nguồn chính thức và liên kết tải về cho CC Switch như sau
[Công tắc FARION1231/CC](https://github.com/farion1231/cc-switch)。
Không tải xuống từ các trang bên thứ ba yêu cầu thanh toán, nạp tiền hoặc bằng chứng tài khoản.
</Warning>
Người dùng Arch Linux có thể cài đặt qua AUR (bảo trì cộng đồng, không phải gói phát hành chính thức của CC Switch):

```bash
paru -S cc-switch-bin
```

## 3. Thêm nhà cung cấp NexusAPI

Sau khi mở CC Switch, vào trang quản lý Nhà cung cấp và chọn Thêm Nhà cung cấp.

Quy trình tổng thể như sau:

```text
打开 CC Switch
-> 新增 Provider
-> 选择要配置的应用
-> 填写 NexusAPI Endpoint、API Key、模型 ID
-> 保存 Provider
-> 切换到该 Provider
-> 启动对应 CLI 工具验证
```

![CC Switch chính thức bổ sung giao diện nhà cung cấp; Khi tích hợp NexusAPI, vui lòng chọn biểu tượng cấu hình tùy chỉnh ở góc trên bên trái](/images/nexusapi/cc-switch-add-provider-official.png)

> Nguồn hình ảnh
> [Kho chính thức của CC Switch](https://github.com/farion1231/cc-switch)，
> Sử dụng theo Giấy phép MIT (© 2025 Jason Young). Các nhà cung cấp khác trong hình ảnh chỉ là ví dụ về giao diện chính thức;
> Khi tích hợp NexusAPI, vui lòng chọn "Cấu hình Tùy chỉnh" và nhấp vào bên dưới để điền thông tin.

Dưới đây là ví dụ tương đối đầy đủ về biểu mẫu Nhà cung cấp từ kho lưu trữ chính thức của CC Switch. Nó hiển thị các khóa API, địa chỉ yêu cầu, mô hình mặc định và
Vị trí các tùy chọn giao thức nâng cao; Bên trong bức ảnh `example.com`, tên mẫu và tên gateway chỉ là ví dụ,**Đừng sao chép một cách mù quáng**。

![Ví dụ về biểu mẫu chính thức của CC Switch Provider, Key đã được che giấu](/images/nexusapi/cc-switch-provider-form-official.png)

> Nguồn ảnh:[Kho chính thức của CC Switch](https://github.com/farion1231/cc-switch/blob/main/docs/images/codex-claude-routing/02-claude-codex-provider-form.png)，
> Giấy phép MIT。 Các tham số thực tế của NexusAPI dựa trên bảng cấu hình Claude / Codex ở cuối trang này.

## 4. Cấu hình mã Claude

Nếu bạn muốn quản lý Claude Code qua CC Switch, hãy nhấp vào bên dưới để điền vào Nhà cung cấp mới.

| Lĩnh vực | Khuyến nghị điền vào mẫu này |
| --- | --- |
| Ứng dụng / Ứng dụng | `Claude` |
| Tên gọi | `NexusAPI Claude` |
| Điểm cuối / URL cơ sở | `https://nexusapi.link` |
| Khóa API | `sk-您的 NexusAPI 令牌` |
| Mô hình chính | Các ID mô hình Claude có sẵn cho token hiện tại |
| Mô hình Haiku | Bạn có thể bỏ qua phần trống hoặc điền vào mô hình Claude nhẹ |
| Mô hình Sonnet | Bạn có thể để trống hoặc điền vào mô hình Sonnet |
| Mô hình Opus | Bạn có thể để trống hoặc điền vào mô hình Opus |

Ví dụ:

```text
App: Claude
Name: NexusAPI Claude
Endpoint: https://nexusapi.link
API Key: sk-xxxxxxxx
Primary Model: YOUR_CLAUDE_MODEL_ID
Sonnet Model: YOUR_CLAUDE_MODEL_ID
```

Lưu ý:

1. Không thêm điểm cuối NexusAPI của Claude Code `/v1`。
2. Sau khi cấu hình, chuyển sang Nhà cung cấp trong CC Switch.
3. Mở lại terminal và chạy lại `claude`。

Phương pháp xác minh:

```bash
claude
```

Sau khi nhập mã Claude, gửi một tin nhắn đơn giản:

```text
请用一句话自我介绍
```

## 5. Cấu hình Codex

Nếu bạn muốn quản lý Codex qua CC Switch, hãy nhấp vào bên dưới để điền Nhà cung cấp mới khi thêm Nhà cung cấp.

| Lĩnh vực | Khuyến nghị điền vào mẫu này |
| --- | --- |
| Ứng dụng / Ứng dụng | `Codex` |
| Tên gọi | `NexusAPI Codex` |
| Điểm cuối / URL cơ sở | `https://nexusapi.link/v1` |
| Khóa API | `sk-您的 NexusAPI 令牌` |
| Mô hình chính | Mã token hiện tại có sẵn cho các mô hình ID tương thích OpenAI |

Ví dụ:

```text
App: Codex
Name: NexusAPI Codex
Endpoint: https://nexusapi.link/v1
API Key: sk-xxxxxxxx
Primary Model: YOUR_CODEX_MODEL_ID
```

Lưu ý:

1. Codex sử dụng giao diện tương thích OpenAI, và Endpoint cần có nó `/v1`。
2. Mô hình phải có sẵn trong nhóm API Key.
3. Nếu trang web hiện tại của bạn đã thiết lập một nhóm riêng cho Codex, khuyến nghị sử dụng nhóm này để tạo một API Key trước khi cấu hình.

Phương pháp xác minh:

```bash
codex
```

Sau khi vào Codex, khởi động một tác vụ đơn giản để xác nhận nó trả về bình thường.

## 6. Tra cứu các mẫu Key hiện có

Cách an toàn nhất là truy vấn bằng khóa API hiện tại `/v1/models`。

Ví dụ:

```bash
curl https://nexusapi.link/v1/models \
  -H "Authorization: Bearer sk-您的 NexusAPI 令牌"
```

Trả về từ kết quả `data[].id` Đây là model ID có sẵn cho khóa API hiện tại.

Khi chọn một mô hình, hãy làm theo các bước sau:

1. Claude Code chọn mô hình lớp Claude.
2. Codex chọn các mô hình tương thích OpenAI phù hợp cho lập trình.
3. Không chọn các mẫu mà khóa API hiện tại không khả dụng.

## 7. Sự khác biệt về điểm cuối giữa các công cụ khác nhau

| Dụng cụ | Điểm cuối |
| --- | --- |
| Mã Claude | `https://nexusapi.link` |
| Codex | `https://nexusapi.link/v1` |

Nếu không chắc chắn, hãy ưu tiên điền vào bảng trên.

## 8. Câu hỏi thường gặp

### 8.1 Sau khi chuyển đổi, địa chỉ chính thức vẫn được yêu cầu

Những lý do có thể xảy ra:

1. CC Switch không chuyển sang Nhà cung cấp NexusAPI.
2. Thiết bị đầu cuối đã mở trước khi chuyển mạch và vẫn sử dụng các biến môi trường cũ.
3. Công cụ CLI mục tiêu có cấu hình cục bộ riêng để ghi đè thiết lập CC Switch.

Điều trị:

1. Chuyển Nhà cung cấp lần nữa trong CC Switch.
2. Đóng terminal rồi mở lại.
3. Kiểm tra tệp cấu hình cục bộ của CLI tương ứng.

### 8.2 Khóa API không hợp lệ

Kiểm tra:

1. Khóa API có phải từ bảng điều khiển NexusAPI không?
2. Có phải bởi `sk-` Khởi đầu.
3. Token có được kích hoạt hay không.
4. Kiểm tra xem số dư tài khoản có đủ không.
5. Token hiện tại có giới hạn mô hình không?

### 8.3 Không có mẫu

Kiểm tra:

1. Việc nhóm API Key hiện tại có hỗ trợ mô hình này không?
2. API Key hiện tại có giới hạn về mô hình không?
3. Liệu kênh backend NexusAPI có được bật hay không.
4. Cách sử dụng `/v1/models` Truy vấn mô hình thực tế có sẵn cho Khóa hiện tại.

### 8.4 Nhắc mã Claude `/login`

Không sử dụng Claude Code khi sử dụng NexusAPI `/login` Đăng nhập vào tài khoản chính thức. Cấu hình Nhà cung cấp NexusAPI nên được ghi qua switch CC.

Nếu nó đã được chạy `/login`, khuyến nghị chuyển lại sang CC Switch NexusAPI Provider và khởi động lại terminal.

## 9. Phương pháp sử dụng được khuyến nghị

Khuyến nghị ở giai đoạn này:

1. Thêm nhà cung cấp thủ công bằng CC Switch.
2. Bỏ qua `/v1/models` Truy vấn các mô hình API Key hiện có.
3. Sau khi cấu hình, xác minh yêu cầu đơn giản được sử dụng trước, sau đó các tác vụ phức tạp được thực hiện.
