---
title: "Mô hình, nhóm và giá"
description: "Hiểu Chợ mô hình NexusAPI, nhóm token và giá hiện hành."
---

NexusAPI tính phí theo mức sử dụng thực tế, không có phí thuê bao cố định. Mô hình, giá, nhóm khả dụng và giao thức có thể thay đổi; hãy coi **Chợ mô hình** trong console và kết quả của khóa hiện tại là nguồn thông tin chuẩn, không dùng bảng giá cũ.

## Bốn điều cần xác nhận trong Chợ mô hình

1. **Model ID** chính xác.
2. **Nhóm token** được phép dùng mô hình.
3. **Giá** hiện tại và đơn vị tính.
4. **Giao thức / endpoint**: `openai`, `anthropic` hoặc tạo ảnh.

![Chợ mô hình NexusAPI có thể lọc theo nhà cung cấp và nhóm token](/images/nexusapi/model-marketplace.png)

## Vì sao một mô hình có thể có ở nhiều nhóm

Nhóm xác định kênh, mô hình và quy tắc giá mà key có thể dùng. Cùng một mô hình có thể xuất hiện ở nhiều nhóm. Đừng suy ra chỉ từ tên nhóm; hãy lọc theo nhóm trong Chợ mô hình và kiểm tra thẻ mô hình.

Nên dùng key riêng cho production, thử nghiệm và từng trung tâm chi phí, đặt tên rõ ràng và giới hạn phù hợp. Trước khi triển khai, gọi `/v1/models` để xem chính xác mô hình key đó truy cập được.

## Chi phí được tạo thành như thế nào

Tùy mô hình, token đầu vào, token đầu ra, đọc cache hoặc các đơn vị khác được nêu rõ có thể được tính riêng. Giá cuối cùng là giá trị trong nhật ký sử dụng áp dụng theo quy tắc hiện tại của Chợ mô hình.

| Không nên chỉ dựa vào | Nên dùng |
| --- | --- |
| Ảnh chụp hoặc bảng giá cũ | Thẻ mô hình hiện tại |
| Danh sách toàn bộ mô hình trên website | Phản hồi `/v1/models` của key đó |
| Trạng thái “thành công” trong client | Nhật ký có Request ID, token và chi phí |

Khả năng dùng còn phụ thuộc giao thức: SDK chung thường dùng Chat Completions, Claude Code dùng Messages, Codex CLI dùng Responses. Hãy xem [ma trận khả năng](/vi/developer/capability-matrix) trước.
