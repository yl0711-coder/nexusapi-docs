---
title: "Tạo và bảo mật khóa API"
---

Khóa API là thông tin đăng nhập để gọi giao diện NexusAPI.

## Quy trình tạo ra

1. Đăng nhập vào bảng điều khiển NexusAPI.
2. Truy cập trang Quản lý Token hoặc Quản lý Khóa API.
3. Nhấp để thêm token.
4. Nhập tên token.
5. Chọn các nhóm có sẵn.
6. Đặt hạn ngạch hoặc giới hạn theo ngân sách dự án.
7. Sau khi gửi, sao chép khóa API đã tạo.

## Cài đặt được khuyến nghị

| Đặt mục | Gợi ý |
| --- | --- |
| Tên gọi | Ví dụ, sử dụng tên dự án hoặc tên khách hàng `codex-work` |
| Nhóm | Chọn nhóm chứa mô hình mục tiêu |
| hạn ngạch | Giới hạn trên hợp lý được đặt theo mức sử dụng, có thể điều chỉnh sau này |
| Giới hạn của mô hình | Chỉ kích hoạt khi thực sự cần thiết |
| Danh sách trắng IP | Chỉ bật khi máy chủ có IP thoát cố định |
| Thời hạn hiệu lực | Khuyến nghị đặt thời gian hết hạn cho các khóa kiểm tra tạm thời |

## Cách sử dụng

Chèn khóa API trong quá trình yêu cầu `Authorization` Tiêu đề yêu cầu:

```http
Authorization: Bearer YOUR_API_KEY
```

## Lời khuyên an toàn

- Không ghi khóa API vào kho mã công khai.
- Không gửi toàn bộ khóa API dưới dạng ảnh chụp màn hình, phiếu hỗ trợ hoặc nhật ký trò chuyện.
- Các dự án, thành viên và môi trường khác nhau được khuyến nghị sử dụng các khóa API khác nhau.
- Môi trường sản xuất được đọc qua các biến môi trường hoặc dịch vụ quản lý khóa, không được ghi trực tiếp vào mã.
- Thường xuyên kiểm tra nhật ký sử dụng; Nếu bạn phát hiện nguồn không quen thuộc hoặc chi tiêu bất thường, hãy ngay lập tức vô hiệu hóa Khóa cũ của bạn.
- Nếu nghi ngờ có rò rỉ, trước tiên hãy tắt hoặc xóa khóa cũ, sau đó tạo một khóa mới và cập nhật chương trình sử dụng nó.

## Sự khác biệt giữa API Key và mã truy cập hệ thống

| Giấy chứng nhận | Mục đích |
| --- | --- |
| Khóa API | Gọi giao diện mô hình, thường là `sk-` Khởi đầu |
| Mã truy cập hệ thống | Kiểm tra cách sử dụng tài khoản và các giao diện cấp tài khoản khác |

Hai loại phiếu này không nên trộn lẫn, và không loại voucher nào nên được đưa vào tham số truy vấn URL.
