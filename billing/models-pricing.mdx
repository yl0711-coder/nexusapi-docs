---
title: "模型、分组与计费"
description: "NexusAPI：模型、分组与计费"
---

NexusAPI 按模型和计费模式计费，没有月租，用多少扣多少。文本模型通常按输入、输出和缓存 Token 计费；图片、音频、视频或任务类模型可能按张、按时长或按次计费。具体规则以模型广场的实时说明为准。

## 模型广场（在售模型 / 价格 / 分组）

在售模型、价格、各分组可用的模型，都在控制台**模型广场**，以实时信息为准：

1. 登录 [NexusAPI 控制台](https://nexusapi.link)，点击右上角[模型广场](https://nexusapi.link/pricing)。
2. 左侧可按`模型供应商`和`可用令牌分组`筛选。
3. 右侧即该分组下可用的模型 ID、价格、简介、支持接口等信息。

![NexusAPI 模型广场，可按供应商和令牌分组筛选](/images/nexusapi/model-marketplace.png)

## 如何选择令牌分组

创建令牌时需要选择**令牌分组**。分组决定这把 Key 可以调用哪些模型及对应价格：

- 同一个模型可能出现在多个分组里，按你要用的模型选对应分组即可。
- 不要只根据分组名称猜测；请在模型广场筛选分组并确认目标模型。
- 同一个项目需要多类模型时，可以分别创建 API Key，便于控制额度和核对用量。

![NexusAPI 创建令牌时选择令牌分组](/images/nexusapi/console-create-token.png)

## 费用由什么组成

- **文本模型**：可能分别计算输入 Token、输出 Token 和缓存相关用量。
- **图片模型**：可能受尺寸、质量、生成数量等参数影响，按张或按次计费。
- **音频、视频和任务类模型**：可能按时长、任务或上游返回的计费单位结算。

最终金额以控制台使用日志显示的单次消费和模型广场实时价格为准；不要把文本模型的 Token 计费规则套用到图片或任务模型。

由于模型、价格和可用分组会调整，本页不维护容易过期的静态模型清单。

## 消费查询

- 每次调用的消费明细，可在控制台「日志」页逐条查看。
- 查看输入、输出、缓存和失败记录的含义，请阅读[用量与日志说明](/billing/usage-and-billing)。
