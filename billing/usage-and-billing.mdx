---
title: "用量与日志说明"
description: "NexusAPI：用量与日志说明"
---

控制台使用日志用于查看每次请求的时间、模型、Token、耗时、状态和消费。对账时请先确认筛选时间与时区。

## 常见字段

| 字段 | 含义 |
| --- | --- |
| 时间 | 请求进入平台的时间 |
| 分组 | 本次请求使用的令牌分组 |
| 模型 | 实际记录的模型名称 |
| 输入 | 输入 Token 数；可能单独显示缓存读取 |
| 输出 | 输出 Token 数 |
| 用时 | 从请求开始到结束的总耗时 |
| 首字 | 流式请求收到第一个内容片段前的等待时间 |
| 类型 | 消费、错误等请求结果 |
| 花费 | 本次请求记录的消费金额 |
| Request ID | 用于定位同一次请求的唯一标识 |

## 输入、输出和缓存

- **输入 Token**：提交给模型的提示词、上下文和工具信息。
- **输出 Token**：模型生成的内容。
- **缓存读取**：命中上下文缓存后读取的 Token；计费方式可能与普通输入不同。

不同模型支持的缓存方式和价格可能不同，请以[模型广场](https://nexusapi.link/pricing)实时信息为准。

## 为什么日志会延迟

请求结束、统计汇总和页面查询之间可能存在短暂延迟。刚完成的请求没有立即出现时，请稍后刷新，不要为了查看日志重复发起业务请求。

## 错误记录和消费

失败不等于一定没有消费：未产生可计费结果的错误记录通常为 0，但上游已经处理、客户端中断或客户端自行再次提交时，仍可能出现消费记录。请结合 Request ID、发生时间和最终日志记录核对，不要只根据客户端是否收到内容判断。

怀疑重复扣费或失败扣费时，请提供：

- Request ID
- 发生时间和时区
- 模型名称
- 客户端是否最终收到完整内容

请勿提供完整 API Key。

## 查看与导出

- 日常查看：使用控制台的用量总览和使用日志。
- 明细筛选：按时间、模型、分组等条件查询。

导出或汇总后，仍应保留 Request ID，方便对异常记录进一步核对。
