---
title: "Создание и защита API-ключа"
description: "Создайте ограниченный ключ NexusAPI, храните его безопасно и заменяйте при риске утечки."
---

API-ключ идентифицирует вызывающую сторону и определяет доступные группы моделей. Создавайте отдельный ключ для проекта или окружения: так проще ограничивать расходы, сверять использование и отключать скомпрометированный доступ.

## Создание ключа

1. Войдите в [консоль NexusAPI](https://nexusapi.link).
2. Откройте **Управление токенами** и нажмите **Добавить токен**.
3. Задайте понятное имя, например `production-api` или `codex-local`.
4. Выберите группу токена с нужной моделью.
5. Укажите лимит и, при необходимости, срок действия, затем сохраните.

Скопируйте ключ сразу: полное значение может показываться только один раз.

## Что определить заранее

| Параметр | Зачем нужен |
| --- | --- |
| Группа токена | Определяет модели, каналы и правила доступа. |
| Лимит кредита | Ограничивает риск расходов проекта или теста. |
| Срок действия | Удобен для временного доступа. |
| Ограничение модели / список IP | Включайте только при необходимости. |

## Безопасное хранение

- Используйте переменные окружения, хранилище секретов или менеджер учётных данных ОС.
- Не размещайте ключ во фронтенде, публичном репозитории, скриншоте, чате или URL.
- Не делите один ключ между несвязанными командами и сервисами.
- При подозрении на утечку немедленно отключите или удалите старый ключ, выпустите новый и обновите развёртывания.

## Тест перед интеграцией

```bash
curl 'https://nexusapi.link/v1/models' \
  -H 'Authorization: Bearer YOUR_API_KEY'
```

Запрос подтверждает ключ и показывает видимые модели. Для выбора API используйте [матрицу возможностей](/ru/developer/capability-matrix).

При 401, 403 или недоступной модели проверьте состояние ключа, группу, кредит, IP-ограничения и формат `Bearer`; сохраните Request ID.
