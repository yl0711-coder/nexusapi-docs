---
title: "Создание и защита ключей API"
---

Ключ API — это пароль для вызова интерфейса NexusAPI.

## Процесс создания

1. Войдите в консоль NexusAPI.
2. Перейдите на страницу управления токенами или управления ключами API.
3. Нажмите, чтобы добавить токен.
4. Введите имя токена.
5. Выберите доступные группы.
6. Устанавливайте квоты или лимиты в соответствии с бюджетом проекта.
7. После отправки скопируйте сгенерированный API-ключ.

## Рекомендуемые настройки

| Поставь предмет | Предложение |
| --- | --- |
| Название | Например, используйте имя проекта или клиента `codex-work` |
| Группировка | Выберите группу, содержащую целевую модель |
| Квота | Разумный верхний предел устанавливается в зависимости от употребления, который позже можно скорректировать |
| Ограничения модели | Активируйте только при явной необходимости |
| Белый список IP | Включать только если сервер имеет фиксированный выходной IP |
| Срок действия | Рекомендуется устанавливать срок действия временных тестовых ключей |

## Как им пользоваться

Вставьте ключ API во время запроса `Authorization` Заголовок запроса:

```http
Authorization: Bearer YOUR_API_KEY
```

## Советы по безопасности

- Не записывайте API-ключи в публичные репозитории кода.
- Не отправляйте полный API Key в виде скриншотов, тикетов или чат-логов.
- Рекомендуется использовать разные API-ключи для разных проектов, участников и сред.
- Производственная среда читается через переменные среды или сервисы управления ключами, а не записывается напрямую в код.
- Регулярно проверяйте журналы использования; Если вы обнаружите незнакомый источник или необычные расходы, немедленно отключите старый ключ.
- Если подозревается утечка, сначала отключите или удалите старый ключ, затем создайте новый ключ и обновите программу, использующую его.

## Разница между API Key и системным токеном доступа

| Сертификат | Назначение |
| --- | --- |
| Ключ API | Вызов интерфейса модели, обычно как `sk-` Начало |
| Системный токен доступа | Проверьте использование аккаунта и другие интерфейсы на уровне аккаунта |

Эти два варианта не следует смешивать, и ни один тип ваучеров не должен включаться в параметр запроса URL.
