---
title: "Использование и списания"
description: "Читайте журналы NexusAPI, токены, Request ID и стоимость отдельных запросов."
---

Сводка и журналы в консоли нужны для сверки запросов, расходов и инцидентов. Новая запись может появиться с небольшой задержкой обработки; не повторяйте рабочий запрос только ради проверки журнала.

## Основные поля

| Поле | Значение |
| --- | --- |
| Время | Время запроса или записи; при сверке сохраняйте часовой пояс. |
| Модель / группа / имя токена | Реально использованный маршрут. |
| Входные токены | Prompt, история и часть данных инструментов. |
| Выходные токены | Содержимое, созданное моделью. |
| Чтение кэша | Использование контекстного кэша; цена может отличаться. |
| Первый байт / общее время | Показатели задержки и таймаутов. |
| Стоимость | Сумма, записанная для запроса. |
| Request ID | Уникальный ID для нахождения того же запроса. |

## Ошибка всегда бесплатна?

Нет. Ошибка без тарифицируемого результата обычно стоит ноль, но обработка могла уже начаться у upstream, поток мог прерваться, или клиент мог отправить новый запрос. Сверяйте **один и тот же Request ID, время и итоговую запись журнала**, а не только видимый результат в клиенте.

## Как сверять

1. Ограничьте период днём или неделей и сохраните часовой пояс.
2. Сопоставьте Request ID из клиента, поддержки и консоли.
3. Сравните модель, группу, входные/выходные токены и стоимость.
4. До агрегации сохраните исходные данные и интервал запроса.

При подозрении на лишнее списание передайте Request ID, время и пояс, модель, итог со стороны клиента и исходную ошибку. Не передавайте полный ключ или конфиденциальный диалог.
