---
title: "Модели, группы и цены"
description: "Понимайте каталог моделей NexusAPI, группы токенов и действующие цены."
---

NexusAPI тарифицирует фактическое использование без фиксированной ежемесячной подписки. Модели, цены, доступные группы и протоколы могут меняться, поэтому ориентируйтесь на **каталог моделей** в консоли и на ответ текущего ключа, а не на старый прайс-лист.

## Четыре вещи в каталоге

1. Точный **ID модели**.
2. Разрешённые **группы токенов**.
3. Текущая **цена** и единица тарификации.
4. Нужный **протокол / endpoint**: `openai`, `anthropic` или генерация изображений.

![Каталог моделей NexusAPI с фильтрами по провайдеру и группе токенов](/images/nexusapi/model-marketplace.png)

## Почему модель может быть в нескольких группах

Группа определяет доступные для ключа каналы, модели и ценовые правила. Одна и та же модель может находиться в нескольких группах. Не делайте вывод по названию группы: отфильтруйте каталог по группе и проверьте карточку модели.

Для production, тестов и разных центров затрат используйте разные ключи, понятные имена и лимиты. Перед развёртыванием выполните `/v1/models`, чтобы увидеть модели, доступные именно этому ключу.

## Из чего складывается стоимость

В зависимости от модели отдельно могут тарифицироваться входные токены, выходные токены, чтение кэша и другие явно указанные единицы. Итоговая сумма — это значение в журнале использования по текущим правилам каталога.

| Не полагайтесь только на | Используйте вместо этого |
| --- | --- |
| Старый скриншот или пересланный прайс | Текущую карточку модели |
| Общий список моделей сайта | Ответ `/v1/models` данного ключа |
| Надпись «успешно» в клиенте | Журнал с Request ID, токенами и стоимостью |

Доступность зависит и от протокола: обычные SDK используют Chat Completions, Claude Code — Messages, Codex CLI — Responses. Сначала прочитайте [матрицу возможностей](/ru/developer/capability-matrix).
