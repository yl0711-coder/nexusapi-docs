---
title: "Обучение по установке и использованию коммутатора CC"
---

CC Switch — это инструмент управления конфигурацией ИИ CLI, который может централизованно управлять конфигурациями API для инструментов AI CLI, таких как Claude Code и Codex. После настройки можно переключаться между разными провайдерами в CC Switch, а затем запускать соответствующий инструмент CLI с помощью NexusAPI.

В этой статье объясняется, как вручную настроить NexusAPI в инструменте CC Switch.

## 1. Подготовка перед использованием

Пожалуйста, подготовьте следующую информацию перед настройкой:

| Проект | Заполните содержимое |
| --- | --- |
| Адрес NexusAPI | `https://nexusapi.link` |
| Ключ API | Токени, созданные консолью NexusAPI, обычно имеют следующий формат: `sk-xxx` |
| Идентификатор модели | Идентификаторы моделей, доступные в текущей группе ключей API |

Путь получения ключей API:

```text
NexusAPI 控制台
-> 令牌管理
-> 创建或复制 API Key
```

Примечание:

1. Ключ API должен быть создан с помощью консоли NexusAPI.
2. Если копированный ключ отсутствует `sk-` Префикс, который нужно добавлять при настройке `sk-xxx`。
3. Идентификаторы моделей должны выбирать модели, доступные в текущей группе токенов; не просто смотрите на список моделей на странице.

## 2. Установите CC-коммутатор

### 2.1 macOS

Рекомендуемая установка с помощью Homebrew:

```bash
brew install --cask cc-switch
```

После установки откройте CC Switch в приложении.

### 2.2 Windows

Посетите страницу релизов CC Switch на GitHub:

```text
https://github.com/farion1231/cc-switch/releases
```

Скачать `.msi` Установите пакет и следуйте инструкциям в установке, чтобы завершить установку.

Если вы используете портативную версию, её тоже можно скачать `.zip` Извлеките сжатый файл и запустите его напрямую.

### 2.3 Linux

Посетите страницу релизов CC Switch на GitHub:

```text
https://github.com/farion1231/cc-switch/releases
```

Скачать по системе `.deb` Пакет установки или `.AppImage` Документы.

<Warning>
**Скачивайте только с официальных каналов**

Официальный исходный код и ссылка для скачивания CC Switch следующие
[farion1231/cc-switch](https://github.com/farion1231/cc-switch)。
Не скачивайте с сторонних страниц, требующих оплаты, пополнения или подтверждения аккаунта.
</Warning>
Пользователи Arch Linux могут устанавливать через AUR (поддержка сообщества, а не официальный пакет релиза CC Switch):

```bash
paru -S cc-switch-bin
```

## 3. Добавлен провайдер NexusAPI

После открытия CC Switch перейдите на страницу управления провайдером и выберите Добавить провайдера.

Общий процесс выглядит следующим образом:

```text
打开 CC Switch
-> 新增 Provider
-> 选择要配置的应用
-> 填写 NexusAPI Endpoint、API Key、模型 ID
-> 保存 Provider
-> 切换到该 Provider
-> 启动对应 CLI 工具验证
```

![CC Switch официально добавила интерфейс поставщика; При интеграции NexusAPI выберите значок пользовательской конфигурации в левом верхнем углу](/images/nexusapi/cc-switch-add-provider-official.png)

> Источник изображения
> [Официальный склад CC Switch](https://github.com/farion1231/cc-switch)，
> Использование по лицензии MIT (© 2025 Jason Young). Другие поставщики, показанные на изображениях, являются лишь официальными интерфейсами;
> При интеграции NexusAPI выберите «Пользовательская конфигурация» и нажмите ниже, чтобы заполнить её.

Ниже приведён относительно полный пример формы Провайдера из официального репозитория CC Switch. В нём представлены ключи API, адреса запросов, модели по умолчанию и
Расположение расширенных вариантов протокола; Внутренняя часть фотографии `example.com`, имена моделей и имена шлюзов — лишь примеры,**Не копируйте вслепую**。

![Пример официальной формы провайдера коммутатора кредитной карты, ключ был замаскирован](/images/nexusapi/cc-switch-provider-form-official.png)

> Источник изображения:[Официальный склад CC Switch](https://github.com/farion1231/cc-switch/blob/main/docs/images/codex-claude-routing/02-claude-codex-provider-form.png)，
> MIT License。 Фактические параметры NexusAPI основаны на таблице конфигурации Claude / Codex внизу этой страницы.

## 4. Настройте код Клода

Если вы хотите управлять кодом Клода через CC Switch, нажмите ниже, чтобы заполнить нового провайдера.

| Поле | Рекомендуется заполнить его |
| --- | --- |
| Приложение / Приложение | `Claude` |
| Название | `NexusAPI Claude` |
| Конечная точка / Базовый URL | `https://nexusapi.link` |
| Ключ API | `sk-您的 NexusAPI 令牌` |
| Основная модель | Идентификаторы модели Claude, доступные для текущего токена |
| Модель хайку | Вы можете пропустить пробел или добавить лёгкую модель Claude |
| Модель сонета | Вы можете оставить его пустым или добавить модель сонета |
| Модель Opus | Вы можете оставить её пустой или заполнить модель Opus |

Пример:

```text
App: Claude
Name: NexusAPI Claude
Endpoint: https://nexusapi.link
API Key: sk-xxxxxxxx
Primary Model: YOUR_CLAUDE_MODEL_ID
Sonnet Model: YOUR_CLAUDE_MODEL_ID
```

Примечание:

1. Не добавляйте NexusAPI Endpoint Claude Code `/v1`。
2. После настройки переключитесь на провайдера в CC Switch.
3. Откройте терминал снова и запустите его снова `claude`。

Метод верификации:

```bash
claude
```

После ввода кода Клода отправьте простое сообщение:

```text
请用一句话自我介绍
```

## 5. Настройте Кодекс

Если вы хотите управлять Codex через CC Switch, нажмите ниже, чтобы заполнить нового провайдера при добавлении провайдера.

| Поле | Рекомендуется заполнить его |
| --- | --- |
| Приложение / Приложение | `Codex` |
| Название | `NexusAPI Codex` |
| Конечная точка / Базовый URL | `https://nexusapi.link/v1` |
| Ключ API | `sk-您的 NexusAPI 令牌` |
| Основная модель | Текущий токен доступен для совместимых с OpenAI ID моделей |

Пример:

```text
App: Codex
Name: NexusAPI Codex
Endpoint: https://nexusapi.link/v1
API Key: sk-xxxxxxxx
Primary Model: YOUR_CODEX_MODEL_ID
```

Примечание:

1. Codex использует интерфейсы, совместимые с OpenAI, и Endpoint должен его иметь `/v1`。
2. Модель должна быть доступна в группе ключей API.
3. Если на вашем текущем сайте создана группа, специфичная для Codex, рекомендуется использовать эту группу для создания ключа API перед его настройкой.

Метод верификации:

```bash
codex
```

После ввода Codex запустите простую задачу, чтобы убедиться, что он возвращается нормально.

## 6. Запросить доступные в настоящее время ключевые модели

Самый безопасный способ — делать запросы с использованием текущего ключа API `/v1/models`。

Пример:

```bash
curl https://nexusapi.link/v1/models \
  -H "Authorization: Bearer sk-您的 NexusAPI 令牌"
```

Результаты результатов `data[].id` Это ID модели, доступный для текущего ключа API.

При выборе модели следуйте следующему:

1. Claude Code выбирает модель класса Claude.
2. Codex выбирает модели, совместимые с OpenAI, подходящие для программирования.
3. Не выбирайте модели, где текущий API Key недоступен.

## 7. Различия в конечных параметрах между различными инструментами

| Инструменты | Конечная точка |
| --- | --- |
| Код Клода | `https://nexusapi.link` |
| Кодекс | `https://nexusapi.link/v1` |

Если не уверены, отдайте приоритет заполнению таблицы выше.

## 8. Часто задаваемые вопросы

### 8.1 После перехода официальный адрес всё ещё запрашивается

Возможные причины:

1. CC Switch не переключился на провайдера NexusAPI.
2. Терминал уже открыт до коммутатора и всё ещё использует старые переменные среды.
3. Целевой инструмент CLI имеет собственную локальную конфигурацию, переопределяющую настройки CC Switch.

Лечение:

1. Переключите провайдера снова в CC Switch.
2. Закройте терминал и откройте его снова.
3. Проверьте локальный конфигурационный файл соответствующего CLI.

### 8.2 Ключ API неверен

Осмотр:

1. Ключ API из консоли NexusAPI?
2. Это по `sk-` Начало.
3. Включён ли токен.
4. Проверьте, достаточно ли остаток счета.
5. Ограничивает ли текущий токен модель?

### 8.3 Модель недоступна

Осмотр:

1. Поддерживает ли текущая группировка ключей API эту модель?
2. Есть ли у текущего API Key ограничения по модели?
3. Включён ли канал NexusAPI backend.
4. Использование `/v1/models` Запросите фактически доступную модель для текущего ключа.

### 8.4 Запрос по коду Клода `/login`

Не используйте Claude Code при использовании NexusAPI `/login` Войдите в официальный аккаунт. Конфигурация NexusAPI Provider должна быть написана через коммутатор CC.

Если он уже был проведён `/login`, рекомендуется вернуться к провайдеру CC Switch NexusAPI и перезапустить терминал.

## 9. Рекомендуемые методы использования

Рекомендации на данном этапе:

1. Вручную добавьте провайдера с помощью CC Switch.
2. Перевал `/v1/models` Запросите доступные в настоящее время модели ключей API.
3. После настройки сначала используется простая проверка запроса, затем выполняются сложные задачи.
