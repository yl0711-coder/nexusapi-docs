---
title: "用量與帳單"
description: "解讀 NexusAPI 使用紀錄、Token、Request ID 與每次請求的費用。"
---

控制台的用量總覽和使用紀錄用於查看請求、花費和異常。剛結束的請求可能有短暫的統計延遲；不要為了確認紀錄而重送業務請求。

## 常見欄位

| 欄位 | 含義 |
| --- | --- |
| 時間 | 請求開始或記錄時間；核對時請同時記下時區。 |
| 模型／群組／令牌名稱 | 此請求實際使用的路由資訊。 |
| 輸入 Token | 提示詞、歷史上下文和部分工具資訊。 |
| 輸出 Token | 模型生成的內容。 |
| 快取讀取 | 命中上下文快取的使用量，計價方式可不同。 |
| 首字延遲／總耗時 | 用於判斷回應速度和超時問題。 |
| 花費 | 此筆記錄記載的實際消費金額。 |
| Request ID | 用來定位同一請求的唯一識別碼。 |

## 輸入、輸出與快取

- **輸入 Token**包含提示詞、對話上下文和部分工具資訊。
- **輸出 Token**是模型生成的內容。
- **快取讀取**是從上下文快取讀取的 Token，用量和價格可能與一般輸入不同。

快取支援與計費方式因模型而異，請以[模型廣場](https://nexusapi.link/pricing)的即時資訊為準。

## 為什麼使用紀錄可能延遲

請求完成、計費寫入和控制台查詢之間可能有短暫延遲。完成的請求暫時看不到時，稍後重新整理即可；不要只是為了產生紀錄而重送原本的業務請求。

## 失敗請求一定不收費嗎？

不一定。未產生可計費結果的錯誤通常會是零，但若上游已開始處理、串流中斷，或用戶端自行再次提交，仍可能留下消費記錄。判斷時請依**同一個 Request ID、發生時間和最終使用紀錄**核對，不要只看畫面是否收到文字。

## 對帳建議

1. 依天或週篩選日期，保留時區。
2. 以 Request ID 匹配用戶端錯誤、支援紀錄和控制台記錄。
3. 比對模型、群組、輸入／輸出 Token 與花費。
4. 匯總前保留原始資料和查詢時間範圍。

## 疑似重複扣費或異常時

請一次提供 Request ID、發生時間與時區、模型、客戶端是否收到完整結果，以及對應錯誤原文。不要提供完整 API Key、密碼或完整敏感對話內容。

## 查看或匯出用量

- 日常檢視可使用控制台總覽與使用紀錄。
- 明細可按時間、模型和群組篩選後匯出或彙總。
- 匯出後請保留 Request ID，便於日後追查異常記錄。

模型價格和群組可見範圍請看[模型、群組與計費](/zh-TW/billing/models-pricing)。
