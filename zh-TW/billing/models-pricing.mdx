---
title: "模型、群組與計費"
description: "了解 NexusAPI 模型廣場、令牌群組、即時價格與可用範圍。"
---

NexusAPI 依實際用量計費，沒有固定月費。模型、價格、可用群組和協定可能調整，因此請以控制台的**模型廣場**和當前 Key 的回傳結果為準，而不是依賴舊的靜態價格表。

## 在模型廣場確認四件事

1. 模型的**準確 ID**。
2. 可使用此模型的**令牌群組**。
3. 此模型的**價格和計費單位**。
4. 對應的**協定／端點**，例如 `openai`、`anthropic` 或圖片生成。

![NexusAPI 模型廣場，可依供應商和令牌群組篩選](/images/nexusapi/model-marketplace.png)

## 為什麼同一模型會顯示於多個群組

群組決定 API Key 可以走的渠道、可用模型和價格規則。同一模型可能在多個群組出現；不要只看群組名稱，而要在模型廣場中篩選群組並確認模型卡片。

建議做法：

- 為正式環境、測試和不同成本中心使用不同 Key。
- 需要多種模型時，以同一專案的明確命名和額度上限管理，不要共用不受控的 Key。
- 先以 `/v1/models` 確認目前 Key 能實際存取的模型。

## 費用如何組成

不同模型可分別計算輸入 Token、輸出 Token、快取讀取或其他明示的用量單位。最終金額以模型廣場當前規則和控制台使用紀錄為準。

| 不要只依賴 | 應該使用 |
| --- | --- |
| 舊的截圖、轉傳價目表或模型別名 | 模型廣場的當前模型卡片 |
| 全站模型清單 | 目前 Key 的 `/v1/models` 結果 |
| 單一請求是否成功 | 使用紀錄中的 Request ID、Token 和花費 |

## 協定同樣會影響可用性

可見的模型不一定可用於每個用戶端。Python 或一般 SDK 常用 Chat Completions；Claude Code 使用 Messages；Codex CLI 使用 Responses。請先讀[介面與模型能力矩陣](/zh-TW/developer/capability-matrix)，再設定用戶端。

每次請求的 Token、快取和金額欄位說明，請看[用量與帳單](/zh-TW/billing/usage-and-billing)。
