---
title: "建立並保護 API 金鑰"
---

API 金鑰是呼叫 NexusAPI 介面的憑證。

## 創建過程

1. 登入 NexusAPI 主控台。
2. 請前往 Token Management 或 API 金鑰管理頁面。
3. 點擊新增代幣。
4. 輸入代幣名稱。
5. 選擇可用群組。
6. 根據專案預算設定配額或限制。
7. 提交後，複製產生的 API 金鑰。

## 推薦設定

| 設定物品 | 建議 |
| --- | --- |
| 名稱 | 例如，使用專案名稱或客戶名稱 `codex-work` |
| 分組 | 選擇包含目標模型的群組 |
| 配額 | 合理的上限會根據使用情況設定，之後可調整 |
| 模型限制 | 只有在明顯需要時才啟動 |
| IP 白名單 | 只有當伺服器有固定的退出 IP 時才啟用 |
| 有效期 | 建議為臨時測試金鑰設定有效期限 |

## 如何使用

在請求過程中插入 API 金鑰 `Authorization` 請求標頭：

```http
Authorization: Bearer YOUR_API_KEY
```

## 安全建議

- 不要將 API 金鑰寫入公開程式碼庫。
- 請勿在截圖、工單或聊天記錄中傳送完整的 API 金鑰。
- 建議不同專案、成員與環境使用不同的 API 金鑰。
- 生產環境是透過環境變數或金鑰管理服務讀取，而非直接寫入程式碼。
- 定期查看使用紀錄; 如果你發現不熟悉的來源或異常支出，請立即停用舊鑰匙。
- 若懷疑有洩漏，先停用或刪除舊金鑰，然後建立新金鑰並更新使用該金鑰的程式。

## API 金鑰與系統存取權杖的差異

| 證書 | 目的 |
| --- | --- |
| API 金鑰 | 呼叫模型介面，通常由 `sk-` 起源 |
| 系統存取權杖 | 檢查你的帳號使用情況及其他帳號層級介面 |

兩者不應混用，且任何一種憑證都不應包含在 URL 查詢參數中。
