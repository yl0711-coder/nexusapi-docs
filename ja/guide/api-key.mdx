---
title: "APIキーの作成と保護"
---

APIキーはNexusAPIインターフェースを呼び出すための認証情報です。

## 作成プロセス

1. NexusAPIコンソールにログインしてください。
2. トークン管理またはAPIキー管理のページに移動します。
3. トークンを追加するにはクリックしてください。
4. トークン名を入力してください。
5. 利用可能なグループを選択します。
6. プロジェクト予算に応じてノルマや上限を設定します。
7. 提出後、生成されたAPIキーをコピーします。

## 推奨設定

| アイテムをセットしてください | 提案 |
| --- | --- |
| 名称 | 例えば、プロジェクト名やクライアント名を使うこと `codex-work` |
| グルーピング | 対象モデルを含むグループを選択します |
| クォータ | 合理的な上限は使用状況に応じて設定され、後で調整可能です |
| モデルの限界 | 明確に必要な時だけ発動します |
| IPホワイトリスト | サーバーが固定の出口IPを持っている場合にのみ有効化してください |
| 有効期間 | 一時的なテストキーには有効期限を設定することが推奨されます |

## 使い方

リクエスト中にAPIキーを挿入してください `Authorization` リクエストヘッダー:

```http
Authorization: Bearer YOUR_API_KEY
```

## 安全アドバイス

- 公開コードリポジトリにAPIキーを書き込まないでください。
- スクリーンショット、チケット、チャットログでAPIキー全文を送信しないでください。
- 異なるプロジェクト、メンバー、環境ごとに異なるAPIキーの使用が推奨されています。
- 本番環境は環境変数や鍵管理サービスを通じて読み込まれ、直接コードに書き込まれるわけではありません。
- 使用履歴を定期的に確認し、 見慣れない情報源や異常な支出を見つけたら、すぐに古いキーを無効にしてください。
- リークが疑われる場合は、まず古いキーを無効化または削除し、その後新しいキーを作成し、それを使用するプログラムを更新します。

## APIキーとシステムアクセストークンの違い

| 証明書 | 目的 |
| --- | --- |
| APIキー | モデルインターフェースの呼び出しは、通常次のように行われます。 `sk-` 始まり |
| システムアクセストークン | アカウントの使用状況やその他のアカウントレベルのインターフェースをチェックしてください |

この2つは混同してはいけず、どちらのタイプのバウチャーもURLクエリパラメータに含めてはいけません。
